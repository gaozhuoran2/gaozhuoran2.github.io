---
title: 学习笔记
date: 2026-07-15 16:11:17
tags: [笔记, 人工智能]
categories: 学习笔记
description: 小白的入门学习
mathjax: true
---

# 线性回归

## 一元线性回归

### 目标

对于给定的数据集 x,y 我们希望用方程$f_{w,b}=wx+b$来拟合 y 与 x 的关系

{% hideToggle 如何用python制表将数据集表示 %}
**1. plt.plot(x, y) —— 画折线图**
```python
plt.plot(x, y, color='red', linestyle='--', marker='o', linewidth=2, label='曲线1') 
```
linestyle：线条样式（-实线、--虚线、:点线）
marker：数据点标记（o圆点、^三角、s方块）
linewidth：线条宽度（数字，越大越粗）
label：曲线标签，配合 plt.legend() 显示图例
**2. plt.bar(x, y) —— 画柱状图**
```python
plt.bar(x, y, color='blue', width=0.8, label='柱状图')
```
**3. plt.scatter(x, y) —— 画散点图**
```python
plt.scatter(x, y, c='blue', marker='o')
```
**4. plt.pie(data) —— 画饼图**
```python
plt.pie(data, labels=labels, autopct='%1.1f%%')
```
data：饼图的数值数据，自动计算占比
labels：每个饼块的分类标签，长度要与data一致
autopct：在扇形内部显示百分比，'%1.1f%% → 保留 1 位小数，'%1.0f%%' → 整数百分比，'%1.2f%%' → 保留两位小数
**5. plt.title() —— 设置图表标题**
```python 
plt.title('图表标题', fontsize=14, color='black')
```
fontsize：标题字体大小
**6. plt.xlabel() plt.ylabel() 设置x,y轴标签**
```python
plt.xlabel('x轴含义', fontsize=12, color='#b03535')
plt.ylabel('y轴含义', fontsize=12, color='#b88f8f')
```
**7. plt.legend() —— 显示图例**
每个图画完后都要写
**8. plt.grid() —— 显示网格**
```python
plt.grid(True, linestyle='--')
```
**9. plt.show() —— 显示图表**
最后统一写，只写一次
**10. plt.figure() —— 新建画布**
一个程序要画多张单图，每张图前要写一遍
**11. 增添中文环境**
在最上方添加
```python
plt.rcParams["font.family"] = "SimHei"
plt.rcParams["axes.unicode_minus"] = False
```

**示例：**
```python
import matplotlib.pyplot as plt

plt.rcParams["font.family"] = "SimHei"
plt.rcParams["axes.unicode_minus"] = False

x=[1,10,3,6]
y=[12,45,2,67]

plt.figure()
plt.plot(x,y,color="blue",linestyle='-',marker='^',linewidth=1,label="折线图")
plt.title('这是折线图',color="black")
plt.xlabel('这是x轴')
plt.ylabel('这是y轴')
plt.legend()

plt.figure()
plt.bar(x,y,color="yellow",width=0.61,label="柱状图")
plt.legend()

plt.figure()
plt.scatter(x,y,c="red",marker='s',label="点图")
plt.legend()

plt.show()
```
{% endhideToggle %}


### 成本函数
对于每一个 w,b 生成的$f_{w,b}=wx+b$我们用成本函数检验拟合函数的优秀程度

$$
J(w,b)=\frac{1}{2m}\sum_{i=0}^{m-1}(f(x_i)-y_i)^2
$$

分别对 w,b 求导得：

$$
\frac{\partial J(w,b)}{\partial w}=\frac{1}{m}\sum_{i=0}^{m-1}(f(x_i)-y_i)x_i
$$

$$
\frac{\partial J(w,b)}{\partial b}=\frac{1}{m}\sum_{i=0}^{m-1}(f(x_i)-y_i)
$$

### 梯度下降
成本函数要求越小越好，显然，延切线方向更新 w,b 即可

$$
w=w-\alpha \frac{\partial J(w,b)}{\partial w}
$$

$$
b=b-\alpha \frac{\partial J(w,b)}{\partial b}
$$

即得最优解

## 多元线性回归

### 向量化

对于每一个样例都有多个维度，每个维度有多个数值，我们可以用向量来表示 $\boldsymbol{w} \ \boldsymbol{x}$ 以此方便计算

{% hideToggle  numpy进行向量计算%}
导入：
```python 
import numpy as np
```
创建数组：
```python
# 1. 列表转数组
arr = np.array([1,2,3,4])
arr2d = np.array([[1,2],[3,4]]) #二维数组

# 2. 全0 / 全1
np.zeros(n)           # 一维
np.zeros((m,n))       # m行n列，必须传元组
np.ones((m,n))

# 3. 单位矩阵
np.eye(n)

# 4. 随机数组
np.random.rand(m,n)     # [0,1)均匀分布
np.random.randn(m,n)    # 标准正态分布 N(0,1)
np.random.randint(low, high, size) # 随机整数
# [low , high) —— 左闭右开  size：输出数组形状  size=5 → 一维数组长度 5   size=(2,3) → 2 行 3 列二维数组
```
数学运算：
```python
a + b, a - b, a * b, a / b   # 逐元素加减乘除
a ** 2                       # 逐元素平方

np.sqrt(arr)     # 开根号
np.exp(arr)      # e^x
np.log(arr)      # ln(x)
np.abs(arr)      # 绝对值
```
聚合函数：
```python
np.sum(arr)      # 全部元素求和
np.sum(arr, axis=0) # 按列求和；axis=1 按行求和
np.mean(arr)     # 平均值
np.max(arr)      # 最大值
np.min(arr)      # 最小值
np.argmax(arr)   # 返回最大值下标
np.argmin(arr)   # 返回最小值下标
```
矩阵乘法：
```python
a @ b          # 矩阵乘法
np.dot(a, b)   # 矩阵乘法，等价 @
# 计算向量时直接相乘即可，计算矩阵乘法需要转置到正确形式
```
{% endhideToggle %}

### 多元梯度下降
与一元梯度下降类似

$$
\frac{\partial J(\boldsymbol{w},b)}{\partial w_j}
= \frac{1}{m}\sum_{i=0}^{m-1}\big(f_{\boldsymbol{w},b}(\boldsymbol{x_i}) - y_i\big)x_i^{(j)}
$$

$$
\frac{\partial J(\boldsymbol{w},b)}{\partial b}
= \frac{1}{m}\sum_{i=0}^{m-1}\big(f_{\boldsymbol{w},b}(\boldsymbol{x_i}) - y_i\big)
$$

$$
w_j = w_j - \alpha \frac{\partial J(\boldsymbol{w},b)}{\partial w_j}
$$

$$
b = b - \alpha \frac{\partial J(\boldsymbol{w},b)}{\partial b}
$$

### 特征缩放

对于每个样例，他的不同特征的数值范围可能差别很大，会影响梯度下降的速度，所以我们进行特征缩放，让每个特征的值都大约分布在 -1 和 1 之间

平均归一化：

$$
x_i^{(j)} = \frac{x_i^{(j)} - \mu_j}{\max - \min}
$$

z-score归一化：

$$
x_i^{(j)} := \frac{x_i^{(j)} - \mu_j}{\sigma_j}
$$

## 多项式回归

